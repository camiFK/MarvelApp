import React from 'react'
import {useSelector, useDispatch} from 'react-redux'

const Landing = () => {

  // const testing = useSelector(state => state.character.characters)
  // console.log(testing)

  return (
    <div>In process</div>
  )
}

export default Landing